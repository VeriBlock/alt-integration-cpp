name: pypoptools

on:
  push:
    branches:
      - master
      - develop
      - release/*
  pull_request:
    branches:
      - master
      - develop
      - trunk/*
      - release/*

jobs:
  install:
    strategy:
      fail-fast: false
      matrix:
        runson: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.runson }}

    steps:
      - uses: actions/checkout@v1
        name: checkout
        with:
          submodules: recursive
          clean: true
      - name: build
        run: python3 setup.py --user